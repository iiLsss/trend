## 1. Update Indicator Styling

- [x] 1.1 Identify the component rendering the gold trend and risk indicators (likely in `app/trends/gold/page.tsx` or a related component).
- [x] 1.2 Remove the solid background color classes (e.g., `bg-green-50`, `bg-orange-50`) from the indicator cards.
- [x] 1.3 Add white background (`bg-white`) and thin border classes (e.g., `border border-gray-200` or matching the Middle East conflict page).
- [x] 1.4 Update text colors for labels and values to match the target editorial aesthetic (e.g., specific red/black colors).
- [x] 1.5 Verify the styling is responsive and looks correct on both desktop and mobile viewports.
